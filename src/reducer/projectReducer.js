import { ADDED, DELETED, UPDATED } from "./action";

export const projectReducer = (state, action) => {
    switch (action.type) {
        case ADDED:
            return


        case UPDATED:
            return
        case DELETED:
            return

        default:
            return state;
    }
}